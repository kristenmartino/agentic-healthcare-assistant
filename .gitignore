# Secrets — never commit
.env

# Generated artifacts
data/*.sqlite
data/*.sqlite-wal
data/*.sqlite-shm
data/*.index
data/*.json
data/*.bin
data/*.npy

# Trace logs (per-invocation; written by tools.tracing)
logs/

# Keep FHIR fixture JSON (checked in as test data); ignore mutable write overlay.
!data/fhir_fixtures/
data/fhir_fixtures/patients_writes.json

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Mac
.DS_Store
**/.DS_Store

# Eval outputs (keep templates, not runs)
eval/results_*.json
eval/adversarial_results_*.json

# Streamlit cache
.streamlit/secrets.toml
